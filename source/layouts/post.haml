- content_for :title do
  #{current_page.title}
!!!
%html{dir: "ltr", lang: "en", "xmlns:fb" => "http://ogp.me/ns/fb#"}
  %head
    = stylesheet_link_tag "syntax_highlighting"
    = partial "shared/meta"
  %body{:class => page_classes}
    = partial "shared/header"
    .container
      #post-main
        %article.post-header
          = partial "shared/article_header", :locals => { :article => current_page }
          .body
            = yield
            = link_to("<-- #{current_page.previous_article.title}", current_page.previous_article) if current_page.previous_article
            = link_to("#{current_page.next_article.title} -->", current_page.next_article) if current_page.next_article
            .tags
              %ol
                - current_page.tags.each do |tag|
                  %li= blog_tag(tag)
            %section.post-footer
              %h1.small-h1.post-author-header
                Our Engineers
                .post-author.clearfix
                  .author-avatar
                    = avatar_for(current_page.data.author)
                  .author-details
                    %h1 Authors Name
                    %span Bio
                    %span.author-posts-link
                      = link_to "View Authors Profile", "/"
    = partial "shared/footer"


